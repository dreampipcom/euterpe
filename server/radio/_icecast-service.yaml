apiVersion: v1
kind: Service
metadata:
  name: icecast
  namespace: euterpe
  labels:
    app: icecast
spec:
  ports:
  - port: 8000
    targetPort: icecast
    protocol: TCP
    name: icecast
  selector:
    app: icecast